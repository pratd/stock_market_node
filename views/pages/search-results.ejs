<% include ../partials/header %>
	<div id="searchResults" class="market-values container py-5">
		<h3 class="mb-3 table-title">Search Results</h3>
		<%if (results.length != 0) { %>
			<div class="accordion" id="searchResult">
				<div class="card">
					<% for(var i=0; i < results.length; i++) { %>
						<div class="card-header" id="<%= 'heading-search-result' + results[i].market_symbol %>">
							<button class="btn-market btn btn-link d-flex justify-content-between align-items-center w-100" type="button" data-toggle="collapse" data-target="<%= '#search-result' + results[i].market_symbol %>" aria-expanded="false" aria-controls="<%= 'search-result' + results[i].market_symbol %>">
								<div class="left-side-els">
									<!-- <img width="36" src="<%= '/images/markets/' + results[i].market_symbol + '.png' %>"> -->
									<span><%= results[i].market_symbol %></span>
									<span><%= results[i].market_name %></span>
								</div>
								<div class="right-side-els d-none d-sm-block d-md-block d-lg-block d-xl-block">
									<!-- <span><%= results[i].close %> $</span> -->
								</div>
							</button>
						</div>
						<div id="<%= 'search-result' + results[i].market_symbol %>" class="collapse" aria-labelledby="<%= 'heading-search-result' + results[i].market_symbol %>" data-parent="#searchResult">
							<div class="card-body d-flex flex-column align-items-end">
								<div>
									<!-- <p class="mb-2 detailed-market-name d-sm-none d-md-none d-lg-none d-xl-none">Last price: $<%= results[0].close %></p> -->
									<p><%= results[i].market_name %> is a stock market in process of growing and therefore VisualMarket recommends it. If you're willing to see how your money grows exponentially, do not hesitate to invest in <%= results[i].market_name %>. 
									Buy stablecoins listed on VisualMarket by wiring money from your account to the providers of these coins. Then, use these stablecoins to buy <%= results[i].market_name %> on VisualMarket exchange.
									VisualMarket supports the deposit of more than 150 cryptocurrencies, several of which you can exchange for <%= results[i].market_name %> at some of the best rates in the market.
									You can directly purchase <%= results[i].market_name %> with credit card on VisualMarket. We offer the best route to buy <%= results[i].market_name %> using debit card or credit card.</p>
								</div>
								<a href="<%= '/market/' + results[i].market_symbol %>"><button class="details-button">Go to detail</button></a>
							</div>
						</div>
					<% }; %>
				</div>
			</div>
		<% }else{ %>
			<p>Sorry, no market name matches your search</p>
		<% } %>
	</div>
<% include ../partials/footer %>